extends layout

block content
    h1= title

    ul
        each author in all_author
            li
                a(href=author.url) #{author.name}
                p #{author.dob_formatted} - #{author.dod_formatted}
     
        else
            li There are no authors

    